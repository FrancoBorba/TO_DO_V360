<!-- Substitui o elemento html com o dom_id(item) , renderizando a partial ou seja
renderizando item de novo quando sofre update e substitui o html antigo pelo novo sem
renderizar a pagina

Foi trocado pois so renderizava o item e agora renderiza toda a lista de novo para ordenar
-->
<%= turbo_stream.replace("todo-items") do %>
  <%= turbo_frame_tag "todo-items" do %>
    <% @item.list.items.order(:concluido, :created_at).each do |item| %>
      <%= render "lists/item", item: item %>
    <% end %>
  <% end %>
<% end %>
